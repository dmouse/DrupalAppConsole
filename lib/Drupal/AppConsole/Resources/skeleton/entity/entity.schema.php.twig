<?php
/**
 * @file
 * Install routines for {{module}} module.
 */

/**
 * Implements hook_schema().
 */
function {{module}}_schema() {
  $schema['{{entity_name}}'] = array(
    'description' => 'Stores {{entity_label}} items.',
    'fields' => array(
      '{{entity_identifier' => array(
        'type' => 'serial',
        'not null' => TRUE,
        'description' => 'Primary Key: Unique {{entity_label}} item ID.',
      ),
      'uuid' => array(
        'description' => 'Unique Key: Universally unique identifier for this entity.',
        'type' => 'varchar',
        'length' => 128,
        'not null' => FALSE,
      ),
      'type' => array(
        'description' => 'The bundle of the {{entity_label}} entity.',
        'type' => 'varchar',
        'length' => 32,
        'not null' => TRUE,
        'default' => '',
      ),
      'langcode' => array(
        'description' => 'The {language}.langcode of the original variant of this {{entity_label}} entity.',
        'type' => 'varchar',
        'length' => 12,
        'not null' => TRUE,
        'default' => '',
      ),
      'name' => array(
        'description' => 'The name of the {{entity_label}} entity.',
        'type' => 'varchar',
        'length' => 32,
        'not null' => TRUE,
        'default' => '',
      ),
      'user_id' => array(
        'type' => 'int',
        'unsigned' => TRUE,
        'not null' => FALSE,
        'default' => NULL,
        'description' => 'The {users}.uid of the associated user.',
      ),
{% for field in fields %}
      '{{field.name}}' => array(
        'description' => '{{field.description}}',
        'type' => 'varchar',
        'length' => 32,
        'not null' => FALSE,
        'default' => '',
      ),
{% endfor %}
    ),
    'primary key' => array('{{entity_identifier}}'),
    'unique keys' => array(
      'uuid' => array('uuid'),
    ),
  );
  return $schema;
}
